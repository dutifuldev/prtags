version: "2"

run:
  timeout: 5m
  tests: false

linters:
  default: none
  enable:
    - cyclop
    - errcheck
    - gocognit
    - ineffassign
    - staticcheck
    - unused
  settings:
    cyclop:
      max-complexity: 30
    gocognit:
      min-complexity: 40

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
